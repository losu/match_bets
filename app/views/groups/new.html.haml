.container
	.w50.center-block
		Zalogowano jako:  
		=current_user.email
		=link_to "wyloguj", destroy_user_session_path, method: :delete 
		%h1.text-center Dodaj grupę
		=form_for(@group) do |f|

			.form-group
			=f.label "Nazwa grupy: " 
			%br
			=f.text_field :name
			=f.submit "Dodaj!", class: "btn, btn-primary"
		
		%br
		%p
		=link_to "Strona Główna", root_url